<% if (post.categories && post.categories.length){ %>
	<%- list_categories(post.categories, {
	  show_count: false,
	  class: 'article-category',
		style: 'none',
		transform: function (t,s,d) {
	  		return '<div class=\'article-category-separator\'>/</div>' + t;
		},
		separator: ''
	}) %>
<% } %>

